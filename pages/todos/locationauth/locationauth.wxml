<cu-custom bgColor="bg-myblue" isBack="{{true}}">
    <view slot="backText">返回</view>
</cu-custom>

<view class="page">
    <camera-face id="cameraFace" equipment="{{equipment}}" bind:noAuth="handleNoAuth" />
</view>